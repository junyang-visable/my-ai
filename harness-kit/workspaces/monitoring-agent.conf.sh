# workspace: monitoring-agent — thin registry (repo path + optional env defaults).
# Commands, notes, and tasks live in the repo itself at docs/harness-kit/.
HARNESS_REPO="/Users/yangjun/Desktop/projects-inc/monitoring-agent"
