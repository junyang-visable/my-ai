{
  "//": "Claude Code 钩子接线片段。install.sh 会把它合并进目标仓库的 .claude/settings.json。",
  "//hooks": "PostToolUse：每次 Edit/Write 后跑增量快检并截断输出；非 0 会把 stderr 回喂给模型。",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash .harness/hooks/post-edit.sh"
          }
        ]
      }
    ]
  }
}
