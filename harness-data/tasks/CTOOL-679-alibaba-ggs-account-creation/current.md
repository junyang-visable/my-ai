# current — current state

> Read this first when resuming in a new session. Keep it to
> "current stage + single next step" only; never pile up history here.
> (State lives in files — the anti-pattern is result.md ballooning to thousands of lines.)

- Task name / ID: CTOOL-679-alibaba-ggs-account-creation (Jira CTOOL-679)
- mode: standard # standard / minimal — minimal must come from an explicit user request (harness-dev §3)
- involved apps: visable-vue · product-editor-frontend (primary) · customer-dashboard-frontend · business-insights-frontend · visitors-frontend · ad-center-frontend · supplier-onboarding-frontend · user-frontend # business-insights/visitors/ad-center/supplier-onboarding not yet registered in harness (§4 onboarding pending)
- spec/plan: /Users/yangjun/Desktop/project/product-editor-frontend/docs/changes/CTOOL-679-alibaba-ggs-account-creation/ # project artifacts, committed with the feature branch
- current stage: `self-test` — **㉗ Task 5 四仓接入+CD pin（2026-09-18，代码完成待 install）**：用户 go-ahead（称 beta.5 已发）+ 点名五仓（company-overview=CD／supplier-onboarding／business-insights／visitors／ad-center）→ Explore×2 侦察后逐仓接入；四新仓均从最新 main/master（BI 主分支是 master）拉分支 `CTOOL-679/alibaba-onboarding-banner`，CD 续用 alibaba-banner 分支：**CD** pin 43.41.1→beta.5（代码 ㉑-㉖ 已完成）；**supplier-onboarding**（Nuxt3/pnpm 设施最全）base.vue 挂载 `:ga4-tracker="$trackingGA4" :ga3-tracker="$tracking"`（语义 wrapper 现成、membership dev+nginx 代理已有）、pin ^43.41.1→beta.5；**BI**（yarn4 勿 pnpm）VisPageHeader 后挂载 `:ga3-tracker="$tracking"`、GA4 不传（应用仅 UA）、visable.d.ts 补 declare、lint:ts+lint:types ✓；**visitors**（npm、GA4 被应用显式禁用）VisBackOfficeLayout slot 前挂载 `:ga3-tracker="$tracking"`、Tracking.ts 补 `show()` 方法（对齐 PE/BI：trackNonInteractionEvent+category 'show'+defaultPayload）+TrackingUtil 接口、/api 通配 devProxy 已覆盖 membership、lint+typecheck ✓；**ad-center**（npm、GA3 被禁用 GA4-only）layout 挂载 `:ga4-tracker="alibabaGa4Tracker"`（适配器映射 banner show/click→`$tracking.trackEvent({event,type:eventTypes.SHOW|CLICK},{event_label,event_position})` 保 customDimension 管道）、types/index.d.ts 补 declare、lint+typecheck ✓。**⚠️ 发版阻塞：v43.44.0-beta.3/4/5 三次 tag 的 Main Release Pipeline 全因 stylelint 失败**（Modal.vue alibaba-wordmark `#ff6600` 违反 color-hex-length——历轮只跑 eslint 漏了 stylelint，CI 的 yarn lint=eslint+stylelint+prettier 三件套）→ registry 无包、五仓 install 全拿不到 beta.5 → 修复 cf597b3c（`#f60` 同值缩写；全套 yarn lint ✓ 123/123 ✓）**已 push（用户拍板"只 push，tag 用户自打 beta.6"）**→ 等 registry 有包后重跑五仓 install+验证。**㉖ GA3 双通道（2026-09-18，已 push）**：用户要求 GA4 之外补 GA3 上报 → tracking.js 增加 `ALIBABA_GA3_EVENTS`（camelCase `{category}_{element}_{action}`，对齐 UF 删除流先例 `click_myAccount_confirmDeleteAlibabaAccountButton`）+ `createAlibabaTracking` 改 `{ga4Tracker, ga3Tracker}` 双通道（随后用户要求显式区分命名，tracker/gaTracker→ga4Tracker/ga3Tracker 全链改名：组件 props、spec fixtures、PE/CD 绑定 `:ga4-tracker`/`:ga3-tracker`）；组件加 `ga4Tracker`/`ga3Tracker` props（ga3Tracker 契约二次纠正：GA4 用富化 wrapper 而 GA3 传裸实例不一致→组件 GA3 通道改发语义方法 `show(action)`/`click(action)`，与 GA4 方法面对称，camelCase action 字符串为唯一载荷，非交互路由与 customDimensions 全部下沉应用 wrapper）；**结果类事件（creation accepted/failed）仅走 GA4**（UF 先例：GA3 只报用户动作）。**PE**：`:ga4-tracker="$trackingGA4" :ga3-tracker="$tracking"`（用户纠正：GA3 用应用语义 wrapper $tracking——Tracking.ts click/show 内部补 customDimensions/trafficSource/referrer 后才落 trackEvent/trackNonInteractionEvent；$googleAnalytics 解构+types/index.d.ts 声明已回退删除）。**CD**：`:ga4-tracker="context.googleAnalytics4" :ga3-tracker="alibabaGa3Tracker"`（CD 裸 GoogleAnalytics 无语义 wrapper→App.vue 内联适配器复用 useTracking(EVENT_TYPE, user, undefined, action) 保维度管道一致；CD 的 show 也走 trackEvent 与其既有惯例一致）。123/123 ✓（GA3 断言改语义方法位置参数）、PE/CD typecheck+lint ✓。**提交：visable-vue 6bbbc9ae（GA3 双通道+ga4/ga3 命名）／PE bc59d53e（$trackingGA4/$tracking 注入）／CD a1da5dd（GA4+useTracking 适配器）**。⚠️ 本地 headless 页 dataLayer 看 GA3 push 不可见（rIC 饿死排除后仍有裸标识符作用域怪异，probe 深挖未竟）——组件→GA3 实例调用链已 spy 实证执行，最终落库待 staging 验证；UF 同款 GA3 用法为生产已验证模式。**㉕ 文案数字修正（2026-09-18，已提交）**：用户要求 150M+→50M+ → banner description + 弹窗 highlight_ep/highlight_wlw（同一事实数字，三处一起改防自相矛盾）×21 语言（保留各语言形态：50M+/50 Millionen/50 milyon…）+ spec en mock 2 处；0 残留、122/122 ✓、浏览器实测 banner 显示 50M+。**㉔ 提交（2026-09-18，未 push）**：visable-vue 123c32b3（㉓ tracker 内聚）+ 36888f7e（㉕ 文案）／PE afb4e165（tracker 集成）／CD afabd9e（㉑+㉓ 合并提交：self-contained 消费+tracker，-294 行）。**㉓ 埋点内聚（2026-09-18）**：用户要求"其他组件上报都在组件层面，banner 埋点也集中到组件中"→ visable-vue 新增 `tracking.js`（ALIBABA_TRACKING_EVENTS 11 事件+position 'alibaba_onboarding'+`createAlibabaTracking(getTracker)`）；组件加 `tracker` prop（对象/null，getter 晚绑定），11 个上报点与 emits 并列内聚（viewed→show、select/popupAccepted/popupAbandoned×3/creationRequested/creationSettled(ACCEPTED|FAILED)/retried/completed/contact→click），emits 全保留、apps 删纯埋点 handler；载荷 `{event:'navigate', event_label, event_position}`（补 PE 原缺的 event 字段，对齐库内 AppSceneBanner 先例；PE 原未绑 creation-settled=首次激活的成功失败从未上报，内聚后统一修复）；spec 新增 funnel-tracking 组 11 用例（spy tracker），122/122 ✓ lint ✓。**PE**：base.vue 挂载缩为 `:brand :locale :fetcher :tracker="$trackingGA4"`，删 10 handler+`tracking` import+tracking.ts ALIBABA 常量（唯一使用方）；eslint ✓ typecheck ✓（exit 0）。**CD**：App.vue 删 ALIBABA_EVENTS+track helpers+内联 handlers，挂载 `:tracker="context.googleAnalytics4"`；vue-tsc ✓ eslint ✓。**浏览器实测（browser-use）**：dataLayer 三事件 banner_viewed(show)/banner_selected(click)/popup_shown(show) 全带 PE 富化维度（platform/user_id/logged_in）——注入契约保数据一致。**PE/CD/visable-vue 均未提交**。**㉒ 客户端崩溃排查+routing 升级（2026-09-18，已提交未 push）**：用户报"onMounted 没执行"（实为 onMounted 无辜、客户端 app 初始化即崩）→ 根因=本地 visable-vue 分支 useNavigationLinks 需要 `SUPPLIER_SETTINGS_ROUTE`，PE 装的 routing@22.7.0 无此导出（vite deps chunk 按 PE 版本出；SSR 走 visable-vue 嵌套新依赖所以服务端 200——"SSR 正常客户端全死"是签名）→ PE bump `@visable-dev/routing`→**22.10.0-beta.1**（exact pin；PE 用到的 8 个导出逐一核对存在）；顺带修 refresh() 全有全无→`Promise.allSettled`（overview 失败才隐藏；BV 403/未部署降级 CTA 形态，符合 spec degrade 要求——/bv/status 两路径均 Tomcat 403，网关/staging 未放行）；browser-use 注入 _user_session cookie（vite @fs 模块 URL 做落点，匿名 200 不走 auth 中间件）实测：banner CTA 可见、overview 200、组件 onMounted 正常；visable-vue 111/111 ✓。**提交：visable-vue 70d419ff（㉑ 重构，6 文件+770）／PE 1c152853（㉑ 集成，-534 行）+ 18c032cd（routing bump）**；用户调试 console.log 已删；pnpm install 第 3 次覆盖 vue 软链已恢复。**㉑ self-contained 组件重构（2026-09-17）**：用户拍板"请求接口逻辑放进组件，长尾应用不能每个抄一套"+两问拍板（请求通道=默认自带+可注入；范围=三仓现在一起改）→ **visable-vue**：新增 `alibabaOnboardingApi.js`（defaultFetcher=same-origin fetch+非2xx throw{status}+空200→undefined；fetcher/locale getter/gatewayBase 可注入；body 预序列化兼容 ofetch）+ `composables/useAlibabaOnboardingState.js`（PE 状态机移植，onMounted 首拉保持零 Nuxt 依赖，in_progress 死分支删除）；组件 `status`/`companies` props 删除→内部状态自隐藏（v-if=visible），create/retry/dismiss/acknowledge 包内，埋点事件全保留+新增 `creation-settled`{status}，viewed 可见时一次性发；3 个 spec 重写，157 文件/1460 测试 ✓ eslint ✓。**PE**：base.vue `:fetcher="$customFetch(url,opts,false)"` external 模式适配器（防路由 X-Supplier-Id 覆盖选中公司）、authStore.isLoggedIn 外层门保留（匿名 401 会跳登录）、handlers 纯埋点、@close 移除；删 app api/composable/ALIBABA_* endpoints/composable spec；spec.md Approach 改写；93/1024 ✓ typecheck ✓。**CD**：App.vue 零注入用默认 fetcher（旧 api 本就是纯 fetch）、ALIBABA_EVENTS+track 留 app 侧；删 composable+api；typecheck ✓（CD 无单测）。**契约变更记录见 [[component-design-single-public-mount]]**。⑱⑲ UF 删除两步弹窗流 + API 4/5 适配（b41deae，已 push）：用户给交互流（理由弹窗→确认弹窗→关闭后按钮变灰+下方撤销文案）并拍板理由走独立接口、wlw 邮箱 kundenservice@wlw.com → AccountDeletion 内嵌 4 个 VisModal（visable 2 + alibaba 2）；新组件 alibaba/AlibabaFeedbackStep.vue（hero=alibaba-delete-hero.svg，Figma 590:6893 导出 cp+cmp 逐字节校验；4 条理由=delete_account.reasons 复用+新 key；理由与备注全空跳过上报直接进确认）+ alibaba/AlibabaConfirmationStep.vue（affirmative 按钮、确认时埋点+requestAlibabaAccountUnlinking）；API 走同源 server route（alibaba-feedback.post.ts→POST /api/feedbacks，surveyName='delete_alibaba_account' 服务端固定；alibaba-account.delete.ts→DELETE ?targetStatus=unlinking，前端只发 unlinking；profileId 经 /api/user-hub/me+cookie 会话解析、Bearer client-credentials server 侧 axiosUserBackendRequest）；alibaba-account.ts git mv 为 alibaba-account.get.ts；unlinking 态=行按钮 disabled+requested_before/after 拆段拼 mailto；GET status fail-closed。**i18n：21 语言 delete_alibaba_account 7 新 key（subtitle/really_wanna_delete/comment_placeholder 复用各语言 Visable 同义句）；用户建议把 unhappy 理由 key 归组→ delete_account.reasons.unhappy_with_request_quality（title 后字母序），4 条理由全同组**；96 文件/946 测试 ✓ eslint ✓ typecheck ✓。⑰ UF 删除卡片按设计稿重排（14ab36f 已 push；Figma 579:5385 两行 VisButton subtle+平台 mark；用户两轮纠正落地：按钮方框叉图标是设计稿 hidden 图层→不实现并删除、subtle 文字色不硬编码随品牌 token；19 语言机翻草稿待母语审校）；UF 资产引用约定 `@/assets/` 非 `~/assets/`（vitest 解析）。⑯ banner 全语言 locale 补齐（2026-09-16）：用户要求"为所有语言加上配置，而不是通过英文回退"→ banner 新增 19 个 locale JSON（es/fr/it/nl/pt/da/sv/no/fi/pl/cs/hu/ro/el/bg/tr/sl/et/lt），21 语言与 shared/constants.js locales 一一对应；新增 `locales.spec.js`（key 树与 en 一致 + 值非空 + consent URL 全语言统一 + 文件集合=支持语言集合），visable-vue 92 测试 ✓ lint ✓（34dd073d）。共享 composable 的 `FALLBACK_LOCALE='en'` 回退保留为防御（仅无效 locale 值可达）。⚠️ 19 语言为机翻草稿待母语审校（consent 法务文案尤甚）。**品牌规则两次演进后定案（b4747c19）**：先按用户口误把品牌拆进 benefit_1（e644796d），随后用户给出 wlw 弹窗设计帧（Figma 558:2316 STEP1 / 560:2369 STEP2，经 Figma REST token 直调解析，~/.qoder/mcp.json）并拍板"品牌进 highlight"→ highlight 拆 `highlight_ep`/`highlight_wlw`（21 语言全拆，品牌名按语法插入），benefit_1 回归单 key 固定 europages（与两版设计一致）；de highlight_wlw 用设计稿逐字文案（含 "(wlw)" 括注与新句式 "und somit die Möglichkeit"，其余 18 语言无括注待审校确认）；弹窗 `highlightCopy` 按 brand 选取、benefit_1 回到 `localized.benefit_1`；spec 品牌用例改断言 highlight+benefit_1，94 测试 ✓ lint ✓。**wlw 蓝主题已落地（1c667d70）**：用户拍板"其他弹窗延续 wlw 颜色样式及 logo，文案与 ep 保持一致"→ 弹窗内容容器/lockup 加 brand class，`.wlw` 作用域覆盖：正文/consent/never-again `#4c5e76`、文字链接/consent 链接 `#0060df`、选中公司卡 `#e5effb`+`#0060df` 边框、卡边框 `#b2bac4`、radio 圆改 currentColor（ep 默认 green-100 / wlw `#0060df`）、主按钮经 VisButton CSS 变量 `--bg--primary/--bg__hover--primary` 覆盖为 `#0060df`/`#0053c4`、lockup 箭头 navy-100、wlw 圆点改 green-100（设计实测值，token 已核对 #7faf0d/#00193c）；success/failure 告警条保持语义绿/红（按钮仍随主题变蓝）；**Badge（Step 1/2）在设计帧里是 visible=false 隐藏图层，不实现**（先前"560 也标 Step 1/2"疑点消解）；文案与 ep 共用无新增 key（consent 维持三段式带 rulechannel 链接，未采用设计稿无链接版式）。94 测试 ✓ eslint/stylelint ✓。锁标 Alibaba 段改文字（496e5c72，用户指出是文案非 SVG）：`cta-banner-icon.svg` img → `<span class="alibaba-wordmark">Alibaba</span>`（display-600 + #ff6600 原始 hex，token orange-100=#ff8700 不符设计值；banner 本体 logo 不动）。Contact us 分平台 mailto（8ec50cfb，用户给定）：under_review 按钮加 `:href="contactHref"`（wlw→kundenservice@wlw.com，非 wlw→customercare@europages.com，VisButton href 渲染为 `<a>`，@contact 埋点保留；PE/CD handler 均纯埋点无冲突）；spec +1 href 双品牌断言，90 测试 ✓。⑮ 切语言消失根因=组件 locales 缺文件致共享 useTranslations 抛错卸载，已修 en 回退（d1b96f5d）；本地调试链：`pnpm dev` 会覆盖 `@visable-dev/vue` 软链→relink 后 `pnpm exec nuxi dev`、清 `node_modules/.vite`+`.nuxt/dev` 缓存、URL 带 `/products-services` 后缀。本轮之前：⑭ UF 删除入口切 IAM 契约（6429ff3：server route alibaba-account.ts 经 axiosUserBackendRequest，404→null/active/unlinking 映射，940 测试 ✓）、⑬ BV pending banner（visable-vue 9b092b7a / PE 9f47a735 / CD f0c1647）、⑫ consent 链接 d015f395（已 push）、⑪ 78px e7526caf、⑩ 双发 22a83cda、⑨ 清零 163126a2、⑧ mock c68b6815、网关直连 88740ae3/58a7ba4、staging 协议 1b1d8b5b/c1a647bf/5657473、client-only 44405e4d、弹窗成败态 63b9ee00/5533e9ca/73e22d7、间距 3f8fa685
- single next step: ㉑-㉖ 全部已 push（visable-vue 6bbbc9ae / PE bc59d53e / CD a1da5dd；PE push 自动追加进 PR #104）→ Task 6 release train — 开 3 条 PR（visable-vue/CD/UF）→ visable-vue tag `v43.45.0-beta.1`（CI publish_npm）→ 各应用精确 pin 升级（见 [[visable-repo-release-via-tag-ci]]；**㉑ 是 breaking 契约：pin 升级 commit 必须与 PE/CD 的 ㉑ 提交同火车；routing 22.10.0-beta.1 同属此火车**）and/or resume Task 5 (4 supplier apps — paused, needs explicit go-ahead)；wlw 侧剩余待办：锁标 wlw 真实 logo 资产替换（当前为文字近似 "wlw."，设计为矢量 logo 实例）+ banner 本体（CTA/under_review teal）是否有 wlw 变体未确认。分支位置：PE bc59d53e（PR #104，已 push）/ CD a1da5dd（已 push）/ visable-vue 6bbbc9ae（已 push）/ user-frontend b41deae（⑲ 已 push）—— **⑬⑭⑮⑯⑰⑱⑲ + ㉑-㉖ 已 push origin**；my-ai harness 记录 commit 未 push（按约定）
- Jira structure (2026-09-12): CTOOL-679 = Epic (relates to CTOOL-624, parent removed); children CTOOL-684 (components, done) / CTOOL-685 (supplier-app integration: product-editor + customer-dashboard done, 4 apps paused) / CTOOL-686 (user-frontend delete entry, done)
- blockers (if any): none — iac 侧 www 域名对 /api/membership-management 的边缘路由仍待确认（customer-dashboard/user-frontend same-origin）；staging IAM 未部署 v3.0 新端点（POST /api/feedbacks、DELETE /api/profiles/{id}/alibaba-account）前，确认步无法端到端联调（报错不卡死）；**/api/membership-management/bv/status 两路径均 403**（Tomcat 拒绝页＝网关边缘路由/staging MMB 未放行，非前端可修）——under_review 形态本地验证不了，前端已按 spec 降级 CTA；另注意 user-frontend 的 CTOOL-679 分支后续需同步网关直连路径
