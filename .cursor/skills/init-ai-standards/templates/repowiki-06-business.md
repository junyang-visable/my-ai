# 06 — 业务知识

## 领域概念

| 术语 | 定义 | 备注 |
|------|------|------|
| [替换: 订单] | [替换: 用户下单后生成的履约单元] | [替换: 与「购物车」区别] |
| [替换: SKU] | [替换: 最小库存单位] | [替换] |
| [替换] | [替换] | [替换] |

## 核心业务流程

### 流程示意（箭头表示先后 / 分支）

```text
[用户打开列表]
    → [筛选条件变更] → [请求列表 API] → [渲染表格]
    → [点击行详情]
          → [加载详情 API]
                → [成功: 展示抽屉]
                → [失败: Toast 错误码 + 可重试]

[提交表单]
    → [前端校验]
          → [失败: 字段级错误]
          → [成功: 调用提交 API]
                → [成功: 关闭弹窗 + 刷新列表]
                → [冲突 / 422: 展示服务端 message]
```

### 关键约束

- [替换: 如「订单关闭后不可再编辑」]
- [替换: 如「退款必须原路返回」]
- [替换: 权限相关：某角色仅可看到某状态的数据]

## 常见误区（AI 易错点）

| 误区（错误理解） | 正确理解 |
|------------------|----------|
| 「取消订单」等于删除数据库记录 | 一般为**状态流转**为已取消，记录保留用于审计 |
| 列表页筛选条件应写进 URL 查询参数 | [替换: 若团队规定仅 session 内有效则写清] |
| 所有错误都用 `message.error` 展示 | [替换: 如 401 统一跳转登录，不与业务 Toast 混用] |
| [替换] | [替换] |

## 外部系统与数据源

| 系统 | 用途 | 前端接触方式 |
|------|------|----------------|
| [替换] | [替换] | [替换: REST / GraphQL / SDK] |

---

- 维护人：[替换]
- 最后更新：[替换: YYYY-MM-DD]
