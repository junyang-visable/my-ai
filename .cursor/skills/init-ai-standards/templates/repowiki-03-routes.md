# 03 — 路由与页面结构

## 页面层级（示例）

以下为**示意结构**；请替换为实际路由树。Nuxt 以 `pages/` 为准，Vue Router 以配置为准。

```text
/                          # 首页或落地页
/login                     # 登录（通常使用独立布局）
/dashboard                 # 登录后默认工作台
/dashboard/
  ├── index                # /dashboard
  ├── settings             # /dashboard/settings
  └── reports/
      └── index            # /dashboard/reports
/[module]/                 # 业务模块动态段示例：/orders/, /customers/
  └── [...]                # [替换: 子路由说明]
/403                       # 无权限提示页
```

| 路径 | 布局 | 说明 |
|------|------|------|
| `/` | default | [替换] |
| `/login` | auth | [替换: 无顶栏侧栏等] |
| `/dashboard/**` | default | [替换] |
| `/403` | minimal | [替换] |

## 路由守卫规则

| 规则 ID | 类型 | 条件 | 行为 | 实现位置 |
|---------|------|------|------|----------|
| G-001 | 认证 | [替换: 未登录] | [替换: 重定向至 /login?redirect=] | [替换: middleware/auth.ts] |
| G-002 | 权限 | [替换: 无某角色/权限] | [替换: /403 或替换为无权限组件] | [替换] |
| G-003 | 重定向 | [替换: 已登录访问 /login] | [替换: 跳转 /dashboard] | [替换] |
| [替换] | [替换: auth / permission / redirect] | [替换] | [替换] | [替换] |

## 布局说明

| 布局文件 | 适用页面 | 包含区域 |
|----------|----------|----------|
| `layouts/default.vue` | 主业务页 | [替换: 顶栏、侧栏、面包屑、footer] |
| `layouts/auth.vue` | 登录、注册、找回密码 | [替换: 居中卡片、无业务导航] |
| [替换: 其他 layout] | [替换] | [替换] |

## 元信息与面包屑

| 字段 | 来源 | 示例 |
|------|------|------|
| `title` | route meta / i18n | [替换] |
| 面包屑 | [替换: 手动 / 自动生成] | [替换: 规则] |

## 深链接与历史记录

- **浏览器回退**： [替换: 列表页是否保留筛选条件]
- **新开标签**： [替换: 是否依赖 sessionStorage 恢复状态]

---

- 维护人：[替换]
- 最后更新：[替换: YYYY-MM-DD]
