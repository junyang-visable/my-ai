# 07 — 测试用例

## 测试环境

| 环境 | URL | 说明 |
|------|-----|------|
| 测试 | [替换: https://test.example.com] | 与后端联调默认环境 |
| 预发 | [替换: https://staging.example.com] | 发布前验收 |
| 本地 | [替换: http://localhost:3000] | E2E 可指向本地或测试环境 |

## 测试账号

| 角色 | 用户名 | 密码 | 权限说明 |
|------|--------|------|----------|
| 普通用户 | [替换] | [替换: 或「见 1Password 条目 xxx」] | [替换] |
| 管理员 | [替换] | [替换] | [替换] |
| 无权限用户 | [替换] | [替换] | 用于负向用例 |

> 密码不得明文提交到仓库；可写「存放位置」或「向 XXX 索取」。

## E2E 测试用例（Playwright）

| 用例 ID | 模块 | 前置条件 | 步骤摘要 | 预期结果 | 优先级 |
|---------|------|----------|----------|----------|--------|
| TC-001 | 登录 | 已在登录页 | 输入合法账号密码 → 点击登录 | 进入 `/dashboard`（或 [替换: 目标路径]），可见 [替换: 关键元素] | P0 |
| TC-002 | 登录 | 已在登录页 | 输入错误密码 → 点击登录 | 停留在登录页，展示明确错误提示（非 500 堆栈） | P0 |
| TC-003 | 鉴权 | 未登录 | 直接访问受保护 URL [替换: /dashboard] | 重定向至 `/login` 且带 `redirect` 参数（若适用） | P0 |
| [替换] | [替换] | [替换] | [替换] | [替换] | [替换] |

## 组件测试用例（Vitest + Vue Test Utils）

| 用例 ID | 组件 | 场景 | 步骤摘要 | 预期 |
|---------|------|------|----------|------|
| CT-001 | `VbButton` | loading | `loading=true` 渲染时点击 | 不触发 `click` emit |
| CT-002 | `VbButton` | disabled | `disabled=true` | 按钮 `aria-disabled` 或不可聚焦（按实现） |
| [替换] | [替换] | [替换] | [替换] | [替换] |

## 回归范围标签（可选）

| 标签 | 含义 | 何时执行 |
|------|------|----------|
| `@smoke` | 冒烟 | 每次主干合并 |
| `@release` | 发布前全量 | [替换] |

---

- 维护人：[替换]
- 最后更新：[替换: YYYY-MM-DD]
